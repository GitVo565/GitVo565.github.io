{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Git Pump",
            "type": "shell",
            "command": "git add .; $status = git status --porcelain; if (-not $status) { Write-Host 'No changes to sync.'; exit 0 }; $date = Get-Date -Format 'yyyy-MM-dd HH:mm'; git commit -m \"automated update: $date\"; git push; if ($?) { [Console]::Beep(880, 300) }",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": true,
                "panel": "shared",
                "showReuseMessage": false,
                "clear": true
            }
        }
    ]
}