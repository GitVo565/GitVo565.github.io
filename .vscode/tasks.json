{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Git Pump",
            "type": "shell",
            "command": "$date = Get-Date -Format 'yyyy-MM-dd HH:mm'; $msg = 'automated update: ' + $date; git add .; if (git diff --staged --quiet) { Write-Host 'Nothing to commit, repository is clean.' } else { git commit -m $msg; git push; if ($?) { [Console]::Beep(880, 300) } }",
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "problemMatcher": []
        }
    ]
}